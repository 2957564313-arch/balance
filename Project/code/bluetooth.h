#ifndef __BLUETOOTH_H__
#define __BLUETOOTH_H__

#include "zf_common_headfile.h"

// 对外控制量
extern int16 remote_speed;                           // 遥控速度目标值
extern int16 remote_turn;                            // 遥控转向目标值
extern volatile uint32 bt_last_joy_ms;               // 最后一次接收到 joystick 帧的时间戳（ms）

// ================================== 对外 API 声明 ==================================

//--------------------------------------------------------------------------------------------------
// 函数简介        蓝牙模块初始化
// 参数说明        void
// 返回参数        void
// 使用示例        bluetooth_init();
// 备注信息        初始化 UART4 接收、回调绑定与解析状态
//--------------------------------------------------------------------------------------------------
void  bluetooth_init(void);

//--------------------------------------------------------------------------------------------------
// 函数简介        蓝牙解析任务（主循环周期调用）
// 参数说明        void
// 返回参数        void
// 使用示例        bluetooth_parse_task();
// 备注信息        从 RX 环形缓冲区取数据并解析完整帧
//--------------------------------------------------------------------------------------------------
void  bluetooth_parse_task(void);

#endif
